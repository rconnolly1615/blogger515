<p id="notice"><%= notice %></p>

<p>
  <strong>Subject:</strong>
  <%= @blog_post.subject %>
</p>

<p>
  <strong>Text:</strong>
  <%= @blog_post.text %>
</p>

<p>
  <strong>Num comments:</strong>
  <%= @blog_post.num_comments %>
</p>

<% if @blog_post.num_comments == 0 || @blog_post.num_comments == nil %>
Sorr, no comments you guys
<br><br>s

<p>
  <strong>Blog:</strong>
  <%= @blog_post.blog_id %>
</p>

<%= @blog_post.blog_comments.each do |blog_comment| %>

<table border>
  <thead>
    <tr>
      <th>Comment</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @blog_comments.each do |blog_comment| %>
      <tr>
        <td><%= blog_comment.comment %></td>
        <td><%= link_to 'Show', blog_comment %></td>
        <td><%= link_to 'Edit', edit_blog_comment_path(blog_comment) %></td>
        <td><%= link_to 'Destroy', blog_comment, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%end%>

This is the form?
<%= form_for[@blog_comment] do |f| %>

  <%= if @blog_comment.errors.any? %>

  <div id="error_explanation">
	
  <h2><%= pluralize(@blog_comment.errors.count, "error" %> prohibiited this blog_comment from being saved:</h2>

	 <ul>

		<%= @blog_comment.errors.full_messages.each do |message| %>
		  <li><%= message %></li>
		<% end %>

		</ul>	
  </div>

<% end %>

	 <div class="field">
    <%= f.label :comment %><br>
    <%= f.text_area :comment %>
    <%= f.hidden_field :blog_post_id, :value => @blog_post_id %>
  </div>

  <div class="actions">
    <%= f.submit %>
  </div>

<%= link_to 'Edit', edit_blog_post_path(@blog_post) %> |
<%= link_to 'Back', blog_posts_path %>
